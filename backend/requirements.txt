# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.0

# Gmail / Google APIs
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.140.0

# HTTP client (model backends)
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Notebook extras (optional, not needed for the server)
jupyter>=1.0.0
pandas>=2.0.0
